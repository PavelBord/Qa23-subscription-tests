[project]
name = "qa23-subscription-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.60.0",
    "pytest>=9.0.3",
    "pytest-playwright>=0.8.0",
]
